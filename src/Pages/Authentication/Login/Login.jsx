import React from "react";
import PropTypes from "prop-types";

const Login = () => {
	return (
		<div>
            Login Page
		</div>
	);
};

Login.propTypes = {
	title: PropTypes.string
};

export default Login;