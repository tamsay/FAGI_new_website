import React from "react";
import PropTypes from "prop-types";

const SignUp = () => {
	return (
		<div>
            Sign Up Page
		</div>
	);
};

SignUp.propTypes = {
	title: PropTypes.string
};

export default SignUp;